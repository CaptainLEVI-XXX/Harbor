query HistoryRequests($series: String!, $after: Bytes!, $first: Int!, $block: Block_height!) {
  _meta(block: $block) { deployment hasIndexingErrors block { number hash } }
  withdrawalRequests(block: $block, first: $first, orderBy: id, orderDirection: asc, where: {series: $series, id_gt: $after}) {
    id series { id } blockNumber blockHash transactionHash transactionIndex logIndex timestamp requestId face shares requestor ownerAtRequest prefixFace prefixShares
  }
}
